language: php

php:
  - 7.2
  - 7.3

before_script:
  - cp .env.travis .env
  - composer self-update
  - composer install --prefer-source --no-interaction --dev
  - composer dump-autoload


cache:
  directories:
    - vendor

script: phpunit
